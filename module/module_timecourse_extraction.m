%% Time-course extraction (overview)
% 1. Number of clusters should be specified (e.g. nROI = 5)
% 2. Naming of ROIs in f'Left/Right/Medial-{RegionName}.mat' (e.g. m-OFC)
% 3. Output should be in the following form 
%     ROI.name = 'm-OFC'
%     ROI.ts   = BOLD [nVox x nTR]




%% Number of clusters should be specified
% Important note: time-course from which preprocessing version should be specified 

nROI = xx; 

for iROI = 1:nROI
    
end


%% Naming of ROIs in f'Left/Right/Medial-{RegionName}.mat' (e.g. m-OFC)
% This process should be done by hand

save_dir = '';
